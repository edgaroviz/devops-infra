terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "5.84.0"
    }
    # backend "s3" {
    # bucket = "mybucket"
    # key    = "path/to/my/key"
    # region = "eu-west-1"
    # }
  }
}

provider "aws" {
  # Configuration options
}

# terraform {
#   backend "s3" {
#     bucket = "mybucket"
#     key    = "path/to/my/key"
#     region = "eu-west-1"
#   }
# }